rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Leads collection - only server can write
    match /leads/{leadId} {
      allow read: if false;
      allow write: if false;
    }
  }
}
